---
alwaysApply: true
---

# AI辅助英语听力训练网站 - 核心开发原则

本文档基于 `docs/` 目录下的项目文档总结，记录项目的核心开发原则和最佳实践。

---

## 1. 测试驱动开发（TDD）- 核心原则

### 测试先行原则
- ✅ **必须先编写测试用例，再实现功能**
- ✅ 测试应该定义期望的行为
- ✅ 测试通过后才算任务完成
- ✅ 测试应该覆盖主要功能和边界情况
- ✅ **所有代码必须经过用户测试验证后才能合并**

### 测试覆盖要求
- 单元测试：测试单个功能
- 集成测试：测试模块间通信
- 回归测试：确保现有功能正常
- 每个服务接口都必须有对应的测试用例

### 测试文件组织
```
module_name/
├── tests/
│   ├── __init__.py          # 必需：使 tests 成为 Python 包
│   ├── test_views.py        # 视图测试
│   ├── test_models.py       # 模型测试
│   └── test_services.py     # 服务测试
└── tests.py                 # ❌ 不应该存在（避免与 tests/ 目录冲突）
```

---

## 2. 架构设计原则

### 2.1 分层架构
```
Views/Controller 层（Django 视图）
    ↓
Service 接口层（common/services/，抽象基类）
    ↓
Service 实现层（各模块的 services/）
    ↓
Models 层（Django ORM）
```

### 2.2 服务接口层设计原则
- **单一职责**：每个服务接口只负责一个业务领域
- **接口隔离**：接口只暴露必要的方法，隐藏实现细节
- **依赖倒置**：其他模块依赖服务接口，而不是具体实现
- **向后兼容**：新接口与旧代码可以共存，逐步迁移

### 2.3 模块通信规范
1. **禁止直接导入其他模块的 models**
   - ❌ 错误：`from Account.models import Students`
   - ✅ 正确：`from common.services.user_service import UserService`

2. **禁止直接导入其他模块的 views**
   - ❌ 错误：`from ELW.views import some_function`
   - ✅ 正确：通过 URL 路由或服务接口调用

3. **通过服务接口访问其他模块的数据**
   - 所有跨模块数据访问必须通过服务接口层

4. **模块内部可以使用自己的 models**
   - ✅ 正确：`ELW/views.py` 中可以 `from ELW.models import MediaMaterial`

---

## 3. 渐进式重构原则

### 3.1 重构基本原则
1. **保证系统可运行**：每一步重构后系统必须能正常运行
2. **向后兼容**：新接口与旧代码可以共存
3. **逐步迁移**：先建立服务接口，再逐步替换旧代码
4. **充分测试**：每个步骤都要有测试验证
5. **严格按计划执行**：不能跳过任何步骤

### 3.2 阶段化开发
- 每个阶段都有明确的依赖关系
- 每个阶段都有明确的验收标准
- 每个阶段的风险都在可控范围内
- 所有阶段都遵循测试驱动开发（TDD）原则

---

## 4. 代码规范

### 4.1 Python 代码规范
- 使用类型提示（`from typing import List, Optional, Dict`）
- 所有服务接口使用 `@staticmethod` 装饰器
- 使用抽象基类（ABC）定义服务接口
- 详细的文档字符串（docstring）

### 4.2 版本兼容性
- 使用 `List[...]` 而不是 `list[...]`（Python 3.8兼容性）
- 使用 `Dict[...]` 而不是 `dict[...]`
- 使用 `Optional[...]` 表示可选类型

### 4.3 命名规范
- 服务接口：定义在 `common/services/<service_name>.py`
- 服务实现：定义在 `<module>/services/<service_name>_impl.py`
- 测试文件：`<module>/tests/test_<功能>.py`

---

## 5. 异常处理规范

### 统一异常体系
所有服务接口使用 `common/exceptions.py` 中定义的统一异常：
- `ServiceException` - 服务层基础异常
- `UserNotFoundError` - 用户不存在异常
- `AuthenticationError` - 认证失败异常
- `PermissionDeniedError` - 权限不足异常
- `ContentNotFoundError` - 内容不存在异常
- `FileOperationError` - 文件操作异常
- `AIServiceError` - AI服务异常
- `NotificationError` - 通知服务异常

### 错误处理原则
- 使用 `None` 检查替代 `DoesNotExist` 异常
- 添加用户友好的错误消息
- 添加必要的权限验证

---

## 6. 文档驱动开发

### 6.1 文档分类
```
docs/
├── 00-项目概览/           # 项目总体信息、架构总结
├── 01-需求与规划/         # 项目需求、开发任务
├── 02-设计文档/           # 架构设计、UI设计
├── 03-开发文档/           # 开发计划、进度、总结
├── 04-开发指南/           # 环境配置、开发规范
├── 05-问题记录/           # 常见问题、已知问题
├── 06-学术文档/           # 学术相关文档
└── 07-历史文档/           # 已归档的历史文档
```

### 6.2 文档更新原则
1. **及时更新**：开发进度、问题记录等文档需要及时更新
2. **版本控制**：重要文档需要标注版本号和更新日期
3. **保持一致性**：相关文档之间保持信息一致
4. **归档历史**：已完成的阶段文档可以归档到历史文档

### 6.3 阶段文档要求
每个阶段都需要包含：
- `阶段N开发计划.md` - 开发计划
- `阶段N进度.md` - 进度跟踪
- `阶段N验收标准.md` - 验收标准
- `阶段N验收操作指南.md` - 验收操作指南
- `阶段N完成总结.md` - 完成总结

---

## 7. Git 工作流规范

### 7.1 分支管理
- 当前开发分支：`dev`
- 每个阶段完成后创建分支标记（如 `stage-2-complete`）
- 充分测试后再合并到主分支

### 7.2 提交规范
- 每个阶段完成后立即提交代码
- 使用清晰的提交信息
- 保持代码库始终处于可运行状态

### 7.3 回滚方案
- 如果发现问题，立即修复（不要等到下一阶段）
- 必要时可以回滚到上一个稳定版本
- 使用 `git reset --hard <commit-hash>` 或创建修复分支

---

## 8. 常见问题预防

### 8.1 Django 测试发现器冲突
- ❌ 不要同时存在 `tests.py` 和 `tests/` 目录
- ✅ 统一使用 `tests/` 目录组织测试文件

### 8.2 URL 命名空间问题
- 检查应用的 `urls.py` 是否定义了 `app_name`
- 如果定义了，使用 `reverse('app_name:url_name')` 格式

### 8.3 模型字段使用
- 编写测试前，先查看模型的实际字段定义
- 参考项目中已有的测试代码
- 不要凭经验猜测字段名

### 8.4 视图前置条件
- 查看视图的完整逻辑，了解所有前置条件
- 在测试中设置必要的 session 变量、登录状态等
- 接受业务逻辑可能返回的所有正常状态码

### 8.5 Python 版本兼容性
- 使用 `from typing import List, Optional, Dict`
- 不要使用 `list[...]`、`dict[...]` 等 Python 3.9+ 语法

---

## 9. 开发模式借鉴（Spring期末项目）

### 9.1 已借鉴的模式
- **分层架构**：清晰的分层设计
- **接口与实现分离**：服务接口与实现分离
- **统一异常处理**：统一的异常体系
- **测试驱动开发**：测试先行原则
- **文档驱动开发**：详细的开发计划和进度

### 9.2 项目特色
- **服务接口层架构**：在 Django 传统架构基础上，引入服务接口层
- **阶段化重构**：采用阶段化的方式逐步重构，降低风险
- **测试先行原则**：所有代码必须经过用户测试验证后才能合并
- **详细文档**：每个阶段都有详细的开发计划、进度跟踪和完成总结

---

## 10. 环境配置原则

### 10.1 环境变量管理
- 使用 `.env` 文件管理环境配置
- `.env.example` 提交到Git，`.env` 不提交
- 使用 `python-decouple` 或 `python-dotenv` 读取配置

### 10.2 数据库迁移规范
1. 创建迁移前，先拉取最新代码
2. 创建迁移：`python manage.py makemigrations`
3. 检查迁移文件是否合理
4. 提交迁移文件到Git
5. 其他开发者拉取后运行 `python manage.py migrate`

### 10.3 开发环境检查清单
- [ ] 使用相同的Python版本（建议3.10+）
- [ ] 使用相同的依赖版本（通过 requirements.txt）
- [ ] 使用相同的数据库配置格式（通过 .env）
- [ ] 定期同步代码（git pull）
- [ ] 运行迁移前先拉取最新代码
- [ ] 测试通过后再提交代码

---

## 11. API开发原则

### 11.1 API文档更新规则
**重要：每次更新后端API时，必须同步更新相关文档**

1. **Swagger文档更新**：
   - 为所有新增或修改的API方法添加 `@extend_schema` 装饰器
   - 包含完整的请求/响应示例
   - 包含错误响应说明（404, 400, 500等）

2. **Postman测试方案文档更新**：
   - 文件位置：`docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段2-API开发/任务2.4-Postman测试方案.md`
   - 添加新的API测试用例到对应的Collection中
   - 更新测试检查清单
   - 更新API端点总数统计

3. **更新内容应包括**：
   - API路径和方法（GET, POST, PUT, DELETE等）
   - 请求参数说明
   - 响应格式示例
   - 错误处理说明
   - 测试步骤

4. **验证步骤**：
   - 重启后端服务后检查Swagger UI是否正确显示
   - 确保Postman测试方案文档中的示例可以正常使用

### 11.2 统一响应格式
- 所有API响应使用 `common/api/response.py` 中定义的统一响应格式
- 成功响应：`ApiResponse.success(data, message)`
- 错误响应：`ApiResponse.error(message, code)`

---

## 12. 最佳实践总结

### 12.1 开发流程
1. 查看需求文档和开发计划
2. **先编写测试用例**（测试驱动开发）
3. 实现功能
4. 运行测试确保通过
5. **等待用户测试验证**
6. 更新进度文档
7. 记录遇到的问题到 `docs/05-问题记录/常见问题记录.md`

### 12.2 代码质量保证
- 遵循测试先行原则
- 严格按照计划执行，不跳过步骤
- 及时修复发现的问题
- 充分测试（单元、集成、回归）
- 文档同步更新

### 12.3 问题处理原则
- 发现问题立即记录到 `docs/05-问题记录/常见问题记录.md`
- 立即修复，不要等到下一阶段
- 保持代码库始终处于可运行状态
- 使用全局搜索确保所有相关代码都已更新

---

## 13. 避免过度工程

### 13.1 代码简洁性原则
- **只做必要的修改**：不要添加未被要求的功能
- **不要过度重构**：bug修复不需要清理周围代码
- **不要添加无用注释**：不要给没修改的代码添加docstring
- **不要添加多余的错误处理**：只在系统边界（用户输入、外部API）验证
- **不要创建无用的抽象**：一次性操作不需要helper函数

### 13.2 避免向后兼容性hack
- 不使用 `_unused_var` 重命名
- 不为删除的代码添加 `// removed` 注释
- 如果代码未使用，直接删除

### 13.3 保持简单
- 三行相似代码比过早抽象更好
- 不为假设的未来需求设计
- 最小复杂度 = 当前任务所需的复杂度

---

## 附录：关键文档路径

### 架构设计
- `docs/00-项目概览/项目架构与开发模式总结.md`
- `docs/02-设计文档/架构设计/阶段二_模块化改进设计.md`

### 开发规范
- `docs/04-开发指南/环境配置/团队开发环境统一方案.md`
- `docs/05-问题记录/常见问题记录.md`

### 阶段文档
- `docs/03-开发文档/模块化重构/阶段1-4/` - 已完成阶段
- `docs/03-开发文档/模块化重构/阶段5/` - 当前阶段
- `docs/03-开发文档/模块化重构/阶段6/` - 待开发阶段

---

**文档版本**: v1.0
**最后更新**: 2025-01-10
**基于文档**: `docs/` 目录下的所有项目文档
