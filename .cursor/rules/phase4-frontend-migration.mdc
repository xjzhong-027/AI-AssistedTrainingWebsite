---
alwaysApply: true
---

# 阶段4：前后端分离 - 剩余页面迁移与功能补齐 AI开发规则

本文档基于 `docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段4-剩余迁移与功能补齐目标.md` 制定，用于指导AI在阶段4开发中的行为规范。

---

## 1. 阶段4核心目标

### 1.1 任务优先级（严格按顺序执行）
1. 教师端周任务（列表、新建、导入、预览）
2. 论坛编辑帖子、我的帖子
3. 考试前确认信息
4. 教师端论坛（帖子详情、删除、置顶）
5. 题库媒体素材详情
6. 任务管理高级（可选）
7. 课程管理
8. 班级管理
9. 题库题目类型/选择/匹配/改错等
10. Query 调课补课
11. 修改密码入口（可选）
12. 文档与代码清理
13. 验收通过

### 1.2 开发模式
- **前后端分离架构**：前端Vue3 + 后端Django REST API
- **API优先**：先确认后端API是否存在，再开发前端
- **渐进式迁移**：保持系统可运行，逐步替换Django模板

---

## 2. 前端开发规范

### 2.1 技术栈
- **框架**：Vue 3 + TypeScript
- **路由**：Vue Router
- **状态管理**：Pinia
- **UI组件库**：Element Plus
- **HTTP客户端**：Axios

### 2.2 文件组织规范
```
frontend/src/
├── views/              # 页面组件
│   ├── Teacher/        # 教师端页面
│   │   ├── WeekTask/   # 周任务相关
│   │   ├── Forum/      # 论坛管理
│   │   ├── QuestionBank/ # 题库管理
│   │   └── ...
│   ├── Student/        # 学生端页面
│   ├── Forum/          # 论坛页面
│   └── Exam/           # 考试页面
├── components/         # 可复用组件
├── api/               # API接口定义
├── router/            # 路由配置
├── stores/            # Pinia状态管理
└── types/             # TypeScript类型定义
```

### 2.3 前端开发流程
1. **检查后端API**：
   - 查看 `<module>/api/views.py` 确认API是否存在
   - 查看 `<module>/api/urls.py` 确认路由配置
   - 查看Swagger文档确认API规格

2. **创建/更新API接口定义**：
   - 在 `frontend/src/api/<module>.ts` 中定义接口函数
   - 使用TypeScript类型定义请求和响应

3. **开发页面组件**：
   - 在 `frontend/src/views/` 对应目录创建Vue组件
   - 使用Composition API（`<script setup lang="ts">`）
   - 使用Element Plus组件

4. **配置路由**：
   - 在 `frontend/src/router/index.ts` 添加路由配置
   - 配置路由守卫（权限检查）

5. **测试功能**：
   - 手动测试所有功能点
   - 确保与后端API正确交互
   - 检查错误处理和用户反馈

### 2.4 前端代码规范
- **组件命名**：使用PascalCase（如 `WeekTaskList.vue`）
- **文件命名**：组件文件使用PascalCase，其他文件使用kebab-case
- **API函数命名**：使用camelCase（如 `getWeekTaskList`）
- **类型定义**：所有API接口必须有TypeScript类型定义
- **错误处理**：使用try-catch捕获API错误，使用Element Plus的Message组件显示错误信息

---

## 3. 后端API开发规范

### 3.1 API开发流程
1. **检查现有API**：
   - 查看 `<module>/views.py` 是否有Django模板视图
   - 查看 `<module>/api/views.py` 是否已有API实现

2. **创建/更新API**：
   - 在 `<module>/api/views.py` 创建ViewSet或APIView
   - 使用DRF的序列化器（`<module>/api/serializers.py`）
   - 添加权限检查（`permission_classes`）

3. **配置路由**：
   - 在 `<module>/api/urls.py` 配置API路由
   - 使用DRF的Router或path配置

4. **添加Swagger文档**：
   - 为所有API方法添加 `@extend_schema` 装饰器
   - 包含完整的请求/响应示例
   - 包含错误响应说明（404, 400, 500等）

5. **更新Postman测试方案**：
   - 文件位置：`docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段2-API开发/任务2.4-Postman测试方案.md`
   - 添加新的API测试用例

### 3.2 API设计原则
- **RESTful风格**：使用标准HTTP方法（GET, POST, PUT, DELETE）
- **统一响应格式**：使用 `common/api/response.py` 中的 `ApiResponse`
- **权限控制**：使用DRF的权限类（`IsAuthenticated`, `IsTeacher`, `IsStudent`）
- **分页**：列表API使用DRF的分页器
- **过滤和搜索**：使用DRF的过滤器和搜索功能

### 3.3 后端代码规范
- **ViewSet命名**：使用 `<Model>ViewSet`（如 `WeekTaskViewSet`）
- **Serializer命名**：使用 `<Model>Serializer`（如 `WeekTaskSerializer`）
- **权限类命名**：使用 `Is<Role>`（如 `IsTeacher`）
- **URL命名**：使用kebab-case（如 `week-task-list`）

---

## 4. 任务执行规范

### 4.1 开始任务前
1. **阅读任务描述**：
   - 查看 `阶段4-剩余迁移与功能补齐目标.md` 中的任务详情
   - 理解 [前端]、[后端]、[功能] 标记的含义

2. **检查依赖**：
   - 确认前置任务是否完成
   - 确认相关文件是否存在

3. **制定计划**：
   - 列出需要创建/修改的文件
   - 确定开发顺序（后端API → 前端接口 → 前端页面 → 路由配置）

### 4.2 开发过程中
1. **后端优先**：
   - 先检查/开发后端API
   - 确保API可以通过Postman测试

2. **前端对接**：
   - 在 `frontend/src/api/` 定义接口函数
   - 在页面组件中调用API

3. **实时测试**：
   - 每完成一个功能点立即测试
   - 确保前后端正确交互

4. **处理TODO**：
   - 解决代码中的TODO注释
   - 移除占位提示和假数据

### 4.3 完成任务后
1. **更新进度文档**：
   - 在 `阶段4-剩余迁移与功能补齐目标.md` 中勾选对应的checkbox
   - 更新 `docs/02-设计文档/前后端分离设计/frontend-backend-separation/前端迁移进度-2026-02.md`

2. **更新API文档**：
   - 确保Swagger文档已更新
   - 确保Postman测试方案已更新

3. **代码清理**：
   - 移除调试代码和console.log
   - 移除未使用的导入
   - 确保代码格式正确

4. **提交代码**：
   - 使用清晰的commit message
   - 遵循Git工作流规范

---

## 5. 具体任务指南

### 5.1 教师端周任务
**涉及文件**：
- 前端：`frontend/src/views/Teacher/WeekTask/index.vue`
- 后端：`ELW/views.py`, `ELW/api/views.py`, `ELW/urls.py`

**开发步骤**：
1. 检查后端是否有周任务相关API
2. 如无API，在 `ELW/api/views.py` 创建 `WeekTaskViewSet`
3. 在 `frontend/src/api/elw.ts` 定义接口函数
4. 更新 `frontend/src/views/Teacher/WeekTask/index.vue`：
   - 移除TODO和占位提示
   - 实现真实的列表展示
   - 添加新建、导入、预览功能
5. 配置路由（如需要）

### 5.2 论坛编辑帖子与我的帖子
**涉及文件**：
- 前端：`frontend/src/views/Forum/Detail.vue`, `frontend/src/views/Forum/Student.vue`
- 后端：`forum/api/views.py`

**开发步骤**：
1. 检查 `forum/api/views.py` 是否有更新帖子API
2. 在 `Forum/Detail.vue` 添加编辑入口和表单
3. 在 `Forum/Student.vue` 添加「我的帖子」筛选
4. 确保仅作者可编辑自己的帖子

### 5.3 考试前确认信息
**涉及文件**：
- 前端：`frontend/src/views/Exam/TakePage.vue`
- 后端：`accessment/views.py`, `accessment/api/views.py`

**开发步骤**：
1. 在进入答题页前增加确认步骤
2. 可以是独立页面或弹窗
3. 展示考试名称、时长、须知
4. 用户确认后才能进入答题

### 5.4 教师端论坛管理
**涉及文件**：
- 前端：`frontend/src/views/Teacher/Forum/index.vue`
- 后端：`forum/api/views.py`

**开发步骤**：
1. 实现帖子详情查看
2. 添加删除帖子功能
3. 添加置顶/取消置顶功能（需检查后端API）

### 5.5 题库媒体素材详情
**涉及文件**：
- 前端：`frontend/src/views/Teacher/QuestionBank/`
- 后端：`ELW/api/views.py`

**开发步骤**：
1. 创建素材详情页或弹窗
2. 展示素材名称、关联题目数、创建时间等
3. 提供组卷、删除等操作入口

---

## 6. 常见问题处理

### 6.1 后端API不存在
**问题**：前端需要的API后端还没有实现

**解决方案**：
1. 检查 `<module>/views.py` 是否有Django模板视图
2. 如有，参考其逻辑在 `<module>/api/views.py` 创建API
3. 如无，询问用户是否需要创建新API

### 6.2 路由冲突
**问题**：新路由与现有路由冲突

**解决方案**：
1. 检查 `frontend/src/router/index.ts` 中的现有路由
2. 使用更具体的路径或嵌套路由
3. 确保路由顺序正确（具体路由在前，通配路由在后）

### 6.3 权限问题
**问题**：API返回403权限错误

**解决方案**：
1. 检查后端API的 `permission_classes`
2. 确保前端发送了正确的认证token
3. 检查用户角色是否正确（教师/学生）

### 6.4 TODO注释处理
**问题**：代码中有TODO注释

**解决方案**：
1. 理解TODO的含义
2. 实现对应功能
3. 移除TODO注释
4. 如果暂时无法实现，在任务文档中记录

---

## 7. 文档更新规范

### 7.1 必须更新的文档
1. **进度文档**：
   - `docs/02-设计文档/前后端分离设计/frontend-backend-separation/前端迁移进度-2026-02.md`
   - 每完成一个功能点立即更新

2. **任务文档**：
   - `docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段4-剩余迁移与功能补齐目标.md`
   - 勾选完成的checkbox

3. **API文档**：
   - Swagger文档（通过 `@extend_schema` 装饰器）
   - Postman测试方案（`docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段2-API开发/任务2.4-Postman测试方案.md`）

### 7.2 文档更新时机
- **开发前**：制定计划时更新任务文档
- **开发中**：完成功能点时更新进度文档
- **开发后**：完成任务时更新所有相关文档

---

## 8. 测试规范

### 8.1 前端测试
- **手动测试**：每个功能点都要手动测试
- **测试内容**：
  - 页面正常渲染
  - 表单验证正确
  - API调用成功
  - 错误处理正确
  - 用户反馈清晰

### 8.2 后端测试
- **API测试**：使用Postman测试所有API
- **测试内容**：
  - 正常请求返回正确数据
  - 错误请求返回正确错误信息
  - 权限控制正确
  - 数据验证正确

### 8.3 集成测试
- **前后端联调**：确保前后端正确交互
- **测试内容**：
  - 完整业务流程可以走通
  - 数据正确传递和显示
  - 错误处理正确

---

## 9. 代码质量要求

### 9.1 前端代码质量
- **TypeScript**：所有代码必须有类型定义
- **组件复用**：相似功能使用可复用组件
- **代码简洁**：避免重复代码
- **注释清晰**：复杂逻辑添加注释
- **格式规范**：使用Prettier格式化代码

### 9.2 后端代码质量
- **类型提示**：使用Python类型提示
- **文档字符串**：所有函数添加docstring
- **异常处理**：使用统一异常体系
- **代码简洁**：遵循DRY原则
- **格式规范**：遵循PEP 8规范

---

## 10. 避免过度工程（继承自development-principles.md）

### 10.1 只做必要的修改
- ❌ 不要添加未被要求的功能
- ❌ 不要过度重构现有代码
- ❌ 不要添加"可能有用"的功能
- ✅ 只实现任务文档中明确要求的功能

### 10.2 保持简单
- ❌ 不要为一次性操作创建helper函数
- ❌ 不要为假设的未来需求设计
- ❌ 不要添加多余的错误处理
- ✅ 最小复杂度 = 当前任务所需的复杂度

### 10.3 代码清理原则
- ✅ 移除未使用的导入
- ✅ 移除调试代码
- ✅ 移除TODO注释（实现后）
- ❌ 不要删除可能有用的现有代码
- ❌ 不要重构未修改的代码

---

## 11. AI开发最佳实践

### 11.1 理解任务
- **仔细阅读**：完整阅读任务描述和涉及文件
- **检查现状**：查看相关文件的当前状态
- **理解依赖**：了解任务的前置条件和依赖关系

### 11.2 制定计划
- **分解任务**：将大任务分解为小步骤
- **确定顺序**：后端 → 前端接口 → 前端页面 → 路由
- **识别风险**：提前识别可能的问题

### 11.3 执行开发
- **小步快跑**：每次只做一个小改动
- **实时测试**：每个改动后立即测试
- **及时反馈**：遇到问题立即报告

### 11.4 完成任务
- **全面测试**：确保所有功能正常
- **更新文档**：更新所有相关文档
- **代码清理**：移除调试代码和TODO
- **提交代码**：使用清晰的commit message

---

## 12. 与用户沟通

### 12.1 何时询问用户
- **需求不明确**：任务描述不清楚时
- **多种方案**：有多种实现方案时
- **缺少信息**：缺少必要的信息时
- **发现问题**：发现潜在问题时

### 12.2 如何询问
- **清晰描述**：清楚描述问题和背景
- **提供选项**：提供可能的解决方案
- **说明影响**：说明不同选择的影响
- **等待确认**：等待用户确认后再继续

---

## 13. 阶段4特殊注意事项

### 13.1 周任务功能
- 周任务是教师端核心功能，需要特别注意
- 导入功能需要处理文件上传和解析
- 预览功能需要展示完整的任务内容

### 13.2 论坛功能
- 编辑功能需要权限检查（仅作者可编辑）
- 置顶功能需要检查后端API是否存在
- 删除功能需要二次确认

### 13.3 考试功能
- 确认信息页面需要展示完整的考试信息
- 用户必须确认后才能进入答题
- 需要防止用户跳过确认步骤

### 13.4 题库功能
- 题目类型多样（选择、匹配、改错等）
- 需要支持媒体素材
- 需要支持题目预览

---

## 附录：关键文件路径

### 前端关键文件
- 路由配置：`frontend/src/router/index.ts`
- API接口：`frontend/src/api/`
- 教师端页面：`frontend/src/views/Teacher/`
- 学生端页面：`frontend/src/views/Student/`
- 论坛页面：`frontend/src/views/Forum/`
- 考试页面：`frontend/src/views/Exam/`

### 后端关键文件
- ELW模块：`ELW/api/views.py`, `ELW/api/urls.py`
- 论坛模块：`forum/api/views.py`, `forum/api/urls.py`
- 考试模块：`accessment/api/views.py`, `accessment/api/urls.py`
- 用户模块：`Account/api/views.py`, `Account/api/urls.py`

### 文档关键文件
- 任务目标：`docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段4-剩余迁移与功能补齐目标.md`
- 进度跟踪：`docs/02-设计文档/前后端分离设计/frontend-backend-separation/前端迁移进度-2026-02.md`
- Postman测试：`docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段2-API开发/任务2.4-Postman测试方案.md`

---

**文档版本**: v1.0
**创建日期**: 2026-02-05
**基于文档**:
- `docs/02-设计文档/前后端分离设计/frontend-backend-separation/阶段4-剩余迁移与功能补齐目标.md`
- `.cursor/rules/development-principles.mdc`
